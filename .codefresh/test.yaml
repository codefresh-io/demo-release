version: '1.0'
steps:
  update_helm_chart:
    image: alpine
    commands:
      - echo "$EVENT_NAME : $EVENT_TAG"
  step1:
    title: Step1
    image: alpine/git
    commands:
     - git status || echo $?
